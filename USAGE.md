# OpenLess Local 使用指南

## 首次配置

### macOS 权限

首次启动后，在「系统设置 → 隐私与安全」中授予以下权限：

1. **麦克风**：允许 OpenLess Local 录音。
2. **辅助功能**：允许 OpenLess Local 向当前焦点输入框插入文字。授权后需要完全退出并重新启动应用，快捷键才会生效。

### Windows 权限

1. 按系统提示授予麦克风权限。
2. 打开 OpenLess Local →「设置 → 权限」，确认全局快捷键监听器状态正常。

### 填入 Provider 凭据

OpenLess Local 不带托管服务，需要你配置自己的 ASR 和 LLM provider。

| 字段 | 说明 |
| --- | --- |
| ASR provider | 语音识别服务或本地 ASR 后端 |
| ASR 凭据 | 例如火山引擎 App ID、Access Token、Resource ID |
| LLM endpoint | 文本润色接口地址 |
| LLM API Key | 文本润色服务凭据 |
| LLM Model ID | 用于润色的模型 |
| Local ASR path | 使用本地识别器时的模型路径 |

火山 ASR 配置见 [docs/volcengine-setup.md](docs/volcengine-setup.md)。Windows 本地 Sherpa ONNX 路径见 [docs/windows-sherpa-onnx-asr-plan.md](docs/windows-sherpa-onnx-asr-plan.md)。

## 基本使用

### 开始录音

按下全局快捷键。默认键位是 macOS 右 Option、Windows 右 Control。屏幕边缘会出现状态胶囊。

### 结束录音

再次按下同一快捷键。OpenLess Local 会停止录音、转写音频、按当前风格润色文本，并插入当前焦点输入框。插入失败时会自动复制到剪贴板。

### 取消录音

录音过程中按 `Esc`，当前录音会被丢弃，不做插入。

## 输出风格

在主窗口或设置中切换风格：

| 风格 | 说明 |
| --- | --- |
| 原文 | 直接输出转写文字 |
| 轻度润色 | 修正语气词、标点和明显错字，保留原意 |
| 清晰结构 | 把口语整理成结构更清楚的表达 |
| 正式表达 | 将口语转换为正式书面语 |
| 自定义风格 | 使用本地创建或导入的风格包 |

风格包只在本地管理，支持导入和导出 ZIP。

## 词汇表

词汇表用于提高专有名词、人名、产品名等内容的识别和润色稳定性。

1. 打开主窗口 →「词汇表」。
2. 新建词条，填写正确拼写、分类和备注。
3. 启用后，词条会参与 ASR 热词和润色提示。

## 历史记录

主窗口 →「历史」可查看本地录音记录、原始转写和润色结果。历史记录保存在本机，可手动删除或清空。

## 更换快捷键

主窗口 →「设置 → 快捷键」可调整录音、打开主窗口和切换风格的快捷键。

## 常见问题

**Q: 快捷键没反应？**

macOS：确认已授予辅助功能权限，且授权后重启过 OpenLess Local。Windows：在「设置 → 权限」中检查监听器状态。

**Q: 识别结果为空？**

检查 ASR provider 配置、网络连接或本地 ASR 模型路径。使用远程 provider 时，凭据和资源 ID 必须正确。

**Q: 文字没有插入，只是复制到了剪贴板？**

某些输入框不支持直接写入。OpenLess Local 会自动回退到剪贴板，手动粘贴即可。

**Q: 本地 ASR 会自动下载模型吗？**

不会在后台自动下载。模型下载或路径选择应由你在设置里主动触发或配置。

**Q: 在 Windows 全屏游戏中无法显示胶囊或插入文字？**

独占全屏应用可能阻止普通桌面窗口绘制和输入注入。优先切换到无边框窗口化全屏，并确认 OpenLess Local 与目标应用使用相同权限级别运行。
